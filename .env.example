# ── Server ────────────────────────────────────────────────
# Port the gateway listens on (default: 8080)
PORT=8080

# Maximum request body size in bytes (default: 1000000 = 1MB)
# MAX_BODY_BYTES=1000000

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# ── vLLM Backend ──────────────────────────────────────────
# Disable TLS certificate verification for vLLM backends (default: true)
# VLLM_TLS_VERIFY=false

# Server profile label for metrics/logging (default: "default")
# VLLM_SERVER_PROFILE=default

# ── Engine Routing (optional, pick one) ───────────────────
# Explicit technique → URL mapping as JSON
# VLLM_BACKEND_MAP_JSON={"baseline":"http://localhost:8000","chunked_prefill":"http://localhost:8001"}

# Auto port-offset routing: baseline=+0, chunked_prefill=+1, speculative=+2, beam_search=+3
# VLLM_AUTO_ENGINE_ROUTING=true

# ── Cost Tracking ─────────────────────────────────────────
# GPU hourly cost in USD for per-request cost estimation (default: 0.0)
# GPU_HOURLY_COST_USD=1.10

# Lambda Cloud API key for automatic pricing lookup (optional)
# LAMBDA_API_KEY=your_key_here

# ── Request Logging ───────────────────────────────────────
# Directory for JSONL request logs (default: logs/gateway, "-" to disable)
# GATEWAY_METRICS_LOG_DIR=logs/gateway

# ── Metrics ───────────────────────────────────────────────
# Prometheus scrape port (default: 9101)
# METRICS_PORT=9101

# ── Load Balancer ─────────────────────────────────────────
# Enable nginx load balancer (clients connect to LB_PORT instead of gateway directly)
# GATEWAY_USE_LOAD_BALANCER=false
# GATEWAY_LB_HOST=localhost
# GATEWAY_LB_PORT=8780

# ── Cloud / SSH Tunnel ────────────────────────────────────
# SSH host for tunnel to remote GPU (e.g. ubuntu@192.168.1.100)
# SSH_HOST=user@gpu-host
# SSH_KEY=~/.ssh/id_ed25519
# LOCAL_PORT=8081
# REMOTE_PORT=8000
